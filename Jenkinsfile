pipeline {
    agent any
    stages {
        stage('BUILD') {
            steps{
               sh 
                echo "build stage"
            } 
        }
        stage ('TEST') {
            steps{
                sh 
                echo "test stage"
            }
         }
        stage ('DEPLOY') {
            steps{
                sh 
                echo "deploy stage"
          }
       }
    }
}
